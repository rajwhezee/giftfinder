## What changed

<!-- One or two sentences. What does this PR do, and why? -->

## How to test

<!-- Steps to verify locally. Include the quiz path or API call if relevant. -->

## Checklist

- [ ] `npm run lint` and `npm run build` pass locally
- [ ] No secrets, API keys or connection strings added to tracked files
- [ ] Any new env var is documented in `.env.example`
- [ ] Schema changes include a Prisma migration (`npm run db:migrate`)
- [ ] Affiliate link changes verified against the merchant's link format
